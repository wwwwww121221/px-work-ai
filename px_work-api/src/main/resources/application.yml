server:
  port: 8081 # 由于8080端口被占用，临时修改为8081
  
spring:
  application:
    name: px_work-api
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.1.51:3306/platrom?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: platrom
    password: "123456"

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # 开启控制台 SQL 打印
  global-config:
    db-config:
      id-type: auto # 配置主键自增
